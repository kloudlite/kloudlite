# Docker Compose Environment Variables

# Host machine IP for API proxy
# This is used to map host.api DNS name to your machine's IP
#
# Options:
# 1. Leave empty to use Docker's host-gateway (works on Docker Desktop)
# 2. Set to your machine's IP address for custom networking
# 3. Use 172.17.0.1 for Docker bridge network on Linux
#
# Examples:
# HOST_IP=192.168.1.100  # Your machine's LAN IP
# HOST_IP=172.17.0.1      # Docker bridge gateway on Linux
# HOST_IP=host.docker.internal  # Docker Desktop (Mac/Windows)
#
HOST_IP=