apiVersion: extensions.kloudlite.io/v1
kind: Cluster
metadata:
  name: kl-01
  namespace: kl-core
spec:
  redpanda:
    adminSecretRef:
      name: msvc-redpanda-admin-creds
      namespace: kl-core
    extraTopicsWithACL:
      - kl-harbor-webhooks

  kubeConfig:
    name: kl-01-kubeconfig
    namespace: kl-core
