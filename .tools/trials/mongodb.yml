apiVersion: crds.kloudlite.io/v1
kind: MongoDBHelm
metadata:
  name: sample-mongo
  namespace: hotspot
spec:
  type: Standalone
  # capacity: 1Gi
  # rootPassword: 
  # auth:
  #   enabled: true
  #   rootPassword: sample
  # # nameOverride: db
  # fullnameOverride: sample-mongo
  # service:
  #   nameOverride: svc
