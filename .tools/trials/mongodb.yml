apiVersion: msvc.kloudlite.io/v1
kind: MongoDB
metadata:
  name: sample
  namespace: hotspot
spec:
  # type: Standalone
  # capacity: 1Gi
  # rootPassword: 
  auth:
    enabled: true
    rootPassword: sample
