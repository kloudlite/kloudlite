apiVersion: crds.kloudlite.io/v1
kind: ManagedService
metadata:
  name: mongo2
  namespace: hotspot
spec
  cloudProvider: do
  msvcKind:
    apiVersion: mongodb-standalone.msvc.kloudlite.io/v1
    kind: Service
  inputs:
    resources:
      cpu:
        min: 300m
        max: 600m
      memory: 600Mi
    storage:
      size: 1Gi
