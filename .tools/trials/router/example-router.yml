apiVersion: crds.kloudlite.io/v1
kind: Router
metadata:
  name: example-router
  namespace: kl-core
spec:
  domains:
    - "ex.devx.khost.dev"
  https:
    enabled: true
  routes:
    - app: httpbin
      rewrite: true
      path: /
      port: 8080
