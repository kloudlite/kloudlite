apiVersion: crds.kloudlite.io/v1
kind: Router
metadata:
  name: auth-api
  namespace: hotspot
spec:
  domains: ['auth-api.dev.kloudlite.io']
  routes:
    /:
      lambda: auth-api
      path: /
      port: 80
