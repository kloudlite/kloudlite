FROM nixos/nix:latest

# Copy the pre-built binary
COPY bin/workmachine-node-manager /usr/local/bin/workmachine-node-manager

# Make it executable
RUN chmod +x /usr/local/bin/workmachine-node-manager

# Run the node manager
ENTRYPOINT ["/usr/local/bin/workmachine-node-manager"]
