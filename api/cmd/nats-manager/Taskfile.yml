version: 3

tasks:
  run:
    cmds:
      - go build -o bin/nats-manager .
      # - ./bin/nats-manager --url "sample" --stream "saf" consumers ls
      - ./bin/nats-manager --url "nats://nats.kloudlite.svc.cluster.local:4222" --stream "resource-sync" consumers ls

  build:
    cmds:
      - go build -o bin/nats-manager .
