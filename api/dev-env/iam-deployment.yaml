apiVersion: apps/v1
kind: Deployment
metadata:
  name: iam-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: iam-api
  template:
    metadata:
      labels:
        app: iam-api
    spec:
      containers:
        - name: iam-api
          workingDir: /var/lib/code/apps/iam
          image: golang:1.23
          command:
            - /bin/sh
            - -c
            - go run main.go --dev
          envFrom:
            - secretRef:
                name: iam-api-env
          volumeMounts:
            - name: code-volume
              mountPath: /var/lib/code
            - name: go-pkg-volume
              mountPath: /go/pkg
      volumes:
        - name: code-volume
          hostPath:
            path: /var/lib/code
            type: DirectoryOrCreate
        - name: go-pkg-volume
          hostPath:
            path: /var/lib/go/pkg
            type: DirectoryOrCreate


---

apiVersion: v1
kind: Service
metadata:
  name: iam-api
spec:
  selector:
    app: iam-api
  ports:
    - protocol: TCP
      name: tcp-8080
      port: 8080
      targetPort: 8080
    - protocol: TCP
      name: tcp-8081
      port: 8081
      targetPort: 8081
  type: ClusterIP