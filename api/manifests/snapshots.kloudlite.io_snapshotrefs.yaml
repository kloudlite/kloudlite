---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: snapshotrefs.snapshots.kloudlite.io
spec:
  group: snapshots.kloudlite.io
  names:
    kind: SnapshotRef
    listKind: SnapshotRefList
    plural: snapshotrefs
    singular: snapshotref
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.snapshotName
      name: Snapshot
      type: string
    - jsonPath: .status.bound
      name: Bound
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          SnapshotRef creates a reference to a Snapshot, incrementing its refCount
          When deleted (manually or via ownerReference), decrements the refCount
          Use ownerReferences to automatically delete when the owning resource is deleted
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SnapshotRefSpec defines which snapshot this ref points to
            properties:
              purpose:
                description: Purpose describes why this reference exists (for documentation)
                type: string
              snapshotName:
                description: SnapshotName is the name of the Snapshot to reference
                type: string
            required:
            - snapshotName
            type: object
          status:
            description: SnapshotRefStatus tracks the reference status
            properties:
              bound:
                description: Bound indicates if the refCount has been incremented
                type: boolean
              boundAt:
                description: BoundAt is when the ref was bound to the snapshot
                format: date-time
                type: string
              snapshotState:
                description: SnapshotState is the current state of the referenced
                  snapshot
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
