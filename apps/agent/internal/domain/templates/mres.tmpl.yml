apiVersion: crds.kloudlite.io/v1
kind: ManagedResource
metadata:
  name: {{.Name}}
  namespace: {{.Namespace}}
spec:
  type: {{.Type}}
  managedSvc: {{.ManagedSvc}}
  values: {{.Values | toJson | squote }}
  version: {{.Version | default 0}}
{{- if .LastApplied }}
  lastApplied: {{ .LastApplied | toJson | squote }}
{{- end }}
