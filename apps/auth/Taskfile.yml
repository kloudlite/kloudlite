version: '3'

dotenv: [".secrets/oauth.env"]

tasks:
  run:
    cmds:
      - nodemon -e go --signal SIGKILL --exec 'go run -tags dynamic main.go --dev || exit 1'
  gql:
    sources:
      - graph/schema.graphqls
      - gqlgen.yml
    dir: internal/app
    cmds:
      - go run github.com/99designs/gqlgen generate

  docker-build:
    vars:
      IMAGE: harbor.dev.madhouselabs.io/kloudlite/auth:v2
      APP: auth
    cmds:
      - docker buildx build -f ./Dockerfile -t {{.IMAGE}} . --build-arg APP={{.APP}} --build-context project-root=../..
      - docker push {{.IMAGE}}

