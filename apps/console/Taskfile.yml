version: "3"

tasks:
  gql:
    dir: ./internal/app
    cmds:
      - go run  -tags dynamic github.com/99designs/gqlgen generate
  run:
    env:
      MONGO_URI: mongodb://console-db:BT09cm-3M493Iix1rY1A3eUjMPITqFtdsMuar7Uk@mongo-standalone.hotspot.svc.cluster.local/console-db
      IMAGE_REGISTRY_PREFIX: harbor.dev.madhouselabs.io/ci
      REDIS_HOSTS: redis-standalone-headless.hotspot.svc.cluster.local:6379
      REDIS_USERNAME: console-redis
      REDIS_PASSWORD: k-weZYsNEgbofBtoPYyoBSVLASOVnQvkwcL-iom3F
      REDIS_PREFIX: console-redis

      REDIS_AUTH_HOSTS: redis-standalone-headless.hotspot.svc.cluster.local:6379
      REDIS_AUTH_USERNAME: redis-auth
      REDIS_AUTH_PASSWORD: ***REMOVED***
      REDIS_AUTH_PREFIX: redis-auth

      LOG_PORT: 8192
      MANAGED_TEMPLATES_PATH: ./managed-svc-templates.yml
      INFRA_HOST: infra-api.hotspot.svc.cluster.local
      INFRA_PORT: 3001
      AUTH_HOST: auth-api.hotspot.svc.cluster.local
      AUTH_PORT: 3001
      CI_HOST: ci-api.hotspot.svc.cluster.local
      CI_PORT: 3001
      IAM_SERVER_HOST: iam.hotspot.svc.cluster.local
      IAM_SERVER_PORT: 3001
      MONGO_DB_NAME: console-db
      PORT: 4001
      GRPC_PORT: 8083
      DEV: true
      KAFKA_BOOTSTRAP_SERVERS: redpanda.hotspot.svc.cluster.local:9092
      KAFKA_INFRA_TOPIC: dev-hotspot-infra
      KAFKA_WORKLOAD_TOPIC: dev-hotspot-agent
      KAFKA_WORKLOAD_RESP_TOPIC: dev-hotspot-agent-reply
      KAFKA_INFRA_RESP_TOPIC: dev-hotspot-infra-resp
      KAFKA_GROUP_ID: console-infra-cli-local
      ORIGINS: http://localhost:4001,https://studio.apollographql.com
      NOTIFIER_URL: http://socket-web-kloudlite.hotspot.svc.cluster.local
      LOKI_URL: http://10.43.172.1:3100/loki/api/v1/tail?limit=30&
      LOG_SERVER_PORT: 8192
    sources:
      - ./internal/**/*.go
      - ./main.go
    cmds:
      - nodemon -e go --signal SIGKILL --exec 'go run -tags dynamic main.go --dev || exit 1'
  docker-build:
    dir: ../..
    vars:
      IMAGE: harbor.dev.madhouselabs.io/kloudlite/console:v2
      APP_DIR: ./apps/console
    cmds:
      - docker build -f ./{{.APP_DIR}}/Dockerfile -t {{.IMAGE}} . --build-arg APP_DIR={{.APP_DIR}}
      - docker push {{.IMAGE}}
