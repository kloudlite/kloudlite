version: 3

tasks:
  run:
    env:
      MONGO_DB_NAME: finance
      GRPC_PORT: 50054
      MONGO_URI: mongodb://mongo.hotspot.svc.cluster.local:27017
      REDIS_HOSTS: redis-headless.hotspot.svc.cluster.local:6379
      PORT: 4002
      IAM_SERVER_HOST: iam.hotspot.svc.cluster.local
      IAM_SERVER_PORT: 3001
      CONSOLE_SERVER_HOST: console-api.hotspot.svc.cluster.local
      CONSOLE_SERVER_PORT: 3001
      CI_SERVER_HOST: ci-api.hotspot.svc.cluster.local
      CI_SERVER_PORT: 3001
      AUTH_SERVER_HOST: auth-api.hotspot.svc.cluster.local
      AUTH_SERVER_PORT: 3001
      COMMS_SERVER_HOST: comms-api.hotspot.svc.cluster.local
      COMMS_SERVER_PORT: 3001
    cmds:
      - nodemon -e go --signal SIGKILL --exec 'go run -tags dynamic main.go --dev || exit 1'
  gql:
    sources:
      - graph/schema.graphqls
      - gqlgen.yml
    dir: internal/app
    cmds:
      - go run github.com/99designs/gqlgen generate

  docker-build:
    dir: ../..
    vars:
      IMAGE: harbor.dev.madhouselabs.io/kloudlite/finance:v2
      APP_DIR: ./apps/finance
    cmds:
      - docker build -f ./{{.APP_DIR}}/Dockerfile -t {{.IMAGE}} . --build-arg APP_DIR={{.APP_DIR}}
      - docker push {{.IMAGE}}
