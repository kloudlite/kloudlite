# kl-tun-proxy image

- Dockerfile: `build-sys/images/kl-tun-proxy/Dockerfile`
- Build context: repository root
- Output image: `ghcr.io/kloudlite/kloudlite/kl-tun-proxy:<tag>`
- Pre-build artifact: built inside the Dockerfile
- Runtime: tunnel proxy binary image.
