# nix-serve image

- Dockerfile: `build-sys/images/nix-serve/Dockerfile`
- Build context: repository root
- Output image: `ghcr.io/kloudlite/kloudlite/nix-serve:<tag>`
- Pre-build artifact: none
- Runtime: serves a Nix binary cache on port `5000`.
