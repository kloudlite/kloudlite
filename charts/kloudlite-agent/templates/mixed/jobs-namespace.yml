apiVersion: v1
kind: Namespace
metadata:
  name: {{.Values.defaults.jobsNamespace}}
  annotations:
    kloudlite.io/description: "this namespace is created and manged by kloudlite platform. It is used to run build and autoscaling jobs"
