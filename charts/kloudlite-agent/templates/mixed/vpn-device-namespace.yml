apiVersion: v1
kind: Namespace
metadata:
  name: {{.Values.operators.agentOperator.configuration.wireguard.deviceNamespace}}
  annotations:
    kloudlite.io/description: "this namespace is created and manged by kloudlite platform. It is used to store VPN Devices"
