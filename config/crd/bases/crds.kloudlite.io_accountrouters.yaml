---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: accountrouters.crds.kloudlite.io
spec:
  group: crds.kloudlite.io
  names:
    kind: AccountRouter
    listKind: AccountRouterList
    plural: accountrouters
    singular: accountrouter
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: AccountRouter is the Schema for the accountrouters API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: AccountRouterSpec defines the desired state of AccountRouter
            properties:
              accountRef:
                type: string
              controllerName:
                type: string
              defaultSSLCert:
                properties:
                  namespace:
                    type: string
                  secretName:
                    type: string
                required:
                - secretName
                type: object
              https:
                properties:
                  clusterIssuer:
                    type: string
                  enabled:
                    default: true
                    type: boolean
                  forceRedirect:
                    type: boolean
                required:
                - enabled
                type: object
              maxBodySizeInMB:
                default: 100
                type: integer
              nodeSelector:
                additionalProperties:
                  type: string
                type: object
              rateLimit:
                properties:
                  connections:
                    type: integer
                  enabled:
                    type: boolean
                  rpm:
                    type: integer
                  rps:
                    type: integer
                type: object
              region:
                type: string
              serviceType:
                enum:
                - ClusterIP
                - LoadBalancer
                type: string
              wildcardDomains:
                items:
                  type: string
                type: array
            required:
            - accountRef
            - region
            - serviceType
            type: object
          status:
            properties:
              checks:
                additionalProperties:
                  properties:
                    generation:
                      format: int64
                      type: integer
                    message:
                      type: string
                    status:
                      type: boolean
                  required:
                  - status
                  type: object
                type: object
              isReady:
                type: boolean
              lastReconcileTime:
                format: date-time
                type: string
              message:
                type: object
                x-kubernetes-preserve-unknown-fields: true
              resources:
                items:
                  properties:
                    apiVersion:
                      description: 'APIVersion defines the versioned schema of this
                        representation of an object. Servers should convert recognized
                        schemas to the latest internal value, and may reject unrecognized
                        values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
                      type: string
                    kind:
                      description: 'Kind is a string value representing the REST resource
                        this object represents. Servers may infer this from the endpoint
                        the client submits requests to. Cannot be updated. In CamelCase.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                  required:
                  - name
                  - namespace
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
