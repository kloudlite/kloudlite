---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: managedservices.crds.kloudlite.io
spec:
  group: crds.kloudlite.io
  names:
    kind: ManagedService
    listKind: ManagedServiceList
    plural: managedservices
    singular: managedservice
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: ManagedService is the Schema for the managedservices API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ManagedServiceSpec defines the desired state of ManagedService
            properties:
              lastApplied:
                description: Contains LastApplied Value for update clauses
                type: string
              templateName:
                description: managed svc template Id
                type: string
              values:
                description: Values define the managed services values that correspond
                  to a particular templateId
                type: string
              version:
                description: Version define the resource version currently in use
                type: integer
            required:
            - templateName
            - values
            - version
            type: object
          status:
            description: ManagedServiceStatus defines the observed state of ManagedService
            properties:
              markedDeleted:
                type: boolean
            required:
            - markedDeleted
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
