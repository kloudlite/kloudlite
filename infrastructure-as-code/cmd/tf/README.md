# tf is a terraform cli

## What does it do ?
It works mostly like terraform only, but reads and writes from encrypted states, and plans.

It is nothing special, just plain `terraform` cli with some configs and 
