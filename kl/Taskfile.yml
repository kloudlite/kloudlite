version: 3

tasks:
  dev:
    cmds:
      - nodemon -q -e 'go' --signal SIGTERM --exec "echo '# building' && task build && echo '# build success' || exit"
  build:
    cmds:
      - go build -ldflags="-X github.com/kloudlite/kl/cmd.Version=v1.0.0-nightly" -o ./bin/kl main.go
