---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: snapshotstores.snapshots.kloudlite.io
spec:
  group: snapshots.kloudlite.io
  names:
    kind: SnapshotStore
    listKind: SnapshotStoreList
    plural: snapshotstores
    singular: snapshotstore
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.registry.endpoint
      name: Registry
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: SnapshotStore defines an OCI registry for storing snapshots
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SnapshotStoreSpec defines the OCI registry configuration
            properties:
              registry:
                description: Registry configures the OCI registry endpoint
                properties:
                  endpoint:
                    description: Endpoint is the registry URL (e.g., "image-registry.kloudlite.svc.cluster.local:5000")
                    type: string
                  insecure:
                    default: true
                    description: Insecure allows HTTP connections (for internal registries)
                    type: boolean
                  repositoryPrefix:
                    default: snapshots
                    description: |-
                      RepositoryPrefix is prepended to all snapshot repositories
                      e.g., "snapshots" results in "snapshots/{owner}/{name}"
                    type: string
                required:
                - endpoint
                type: object
            required:
            - registry
            type: object
          status:
            description: SnapshotStoreStatus defines the observed state
            properties:
              lastChecked:
                description: LastChecked is when connectivity was last verified
                format: date-time
                type: string
              message:
                description: Message provides status details
                type: string
              ready:
                description: Ready indicates if the registry is accessible
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
