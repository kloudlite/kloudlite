apiVersion: crds.kloudlite.io/v1
kind: Router
metadata:
  name: example-web
  namespace: hotspot
spec:
  domains: ['examplex.dev.kloudlite.io']
  https:
    enabled: true
  routes:
    /:
      app: example
      port: 80
