---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: environments.kloudlite.io
spec:
  group: kloudlite.io
  names:
    kind: Environment
    listKind: EnvironmentList
    plural: environments
    singular: environment
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.targetNamespace
      name: target-ns
      type: string
    - jsonPath: .status.lastReconcileTime
      name: Seen
      type: date
    - jsonPath: .metadata.annotations.operator\.kloudlite\.io\/checks
      name: Checks
      type: string
    - jsonPath: .spec.suspend
      name: Paused
      type: boolean
    - jsonPath: .metadata.annotations.operator\.kloudlite\.io\/resource\.ready
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: Environment is the Schema for the environments API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EnvironmentSpec defines the desired state of Environment.
            properties:
              paused:
                description: Paused pauses the environment
                type: boolean
              serviceAccount:
                description: ServiceAccount is used for all apps deployed in this
                  environment
                type: string
              targetNamespace:
                description: TargetNamespace is namespace under which all resources
                  are created
                type: string
            type: object
          status:
            properties:
              checkList:
                items:
                  properties:
                    debug:
                      type: boolean
                    description:
                      type: string
                    hide:
                      type: boolean
                    name:
                      type: string
                    title:
                      type: string
                  required:
                  - name
                  - title
                  type: object
                type: array
              checks:
                additionalProperties:
                  properties:
                    completedAt:
                      format: date-time
                      type: string
                    generation:
                      format: int64
                      type: integer
                    message:
                      type: string
                    startedAt:
                      format: date-time
                      type: string
                    state:
                      type: string
                  type: object
                type: object
              isReady:
                type: boolean
              lastReadyGeneration:
                format: int64
                type: integer
              lastReconcileTime:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
