apiVersion: v1
kind: Service
metadata:
  name: sample-service
  labels:
    app.kubernetes.io/name: sample-service
    app.kubernetes.io/managed-by: kustomize
spec:
  selector:
    app: sample-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP