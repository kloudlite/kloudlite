apiVersion: mongodbs.msvc.kloudlite.io/v1
kind: Database
metadata:
  name: {{.Name}}
  namespace: {{.Namespace}}
  ownerReferences:
    - apiVersion: {{.APIVersion}}
      kind: {{.Kind}}
      name: {{.Name}}
      uid: {{.UID}}
      controller: true
      blockOwnerDeletion: true
spec:
  managed_svc: {{.Spec.ManagedSvc }}
