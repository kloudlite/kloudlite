apiVersion: {{.Spec.ApiVersion}}
kind: Service
metadata:
  name: {{.Name}}
  namespace: {{.Namespace}}
{{- if .Labels }}
  labels:
  {{- range $k, $v := .Labels}}
    {{$k}}: {{$v}}
  {{- end }}
{{- end }}
  ownerReferences:
    - apiVersion: {{.APIVersion}}
      kind: {{.Kind}}
      name: {{.Name}}
      uid: {{.UID}}
      controller: true
      blockOwnerDeletion: true
spec:
  inputs: {{.Spec.Inputs | toJson }}
