apiVersion: v1
kind: Secret
metadata:
  name: {{.Name}}
  namespace: {{.Namespace}}
{{- if .StringData }}
stringData:
  {{- range $key,$value := .StringData}}
  {{$key}}: {{$value}}
  {{- end }}
{{- end }}