# apiVersion: v1
# kind: Service
# metadata:
#   name: gateway-admin-api
#   namespace: "{{.namespace}}"
# spec:
#   type: ClusterIP
#   selector:
#     app: kl-networking
#   ports:
#     - name: http
#       port: 8090
#       targetPort: 8090
---
apiVersion: v1
kind: Service
metadata:
  name: gateway-admin-api
  namespace: "{{.namespace}}"
spec:
  type: ExternalName
  externalName: baby.default.svc.cluster.local
