apiVersion: v1
kind: Service
metadata:
  name: {{ include "kloudlite-platform.fullname" . }}
  labels:
    {{- include "kloudlite-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kloudlite-platform.selectorLabels" . | nindent 4 }}
